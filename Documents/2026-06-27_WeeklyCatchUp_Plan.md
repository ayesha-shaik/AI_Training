# 1-Week Catch-Up Plan — 60 min/day

*Prepared: June 27, 2026 • Goal: walk into the next live session following the lecture confidently*

**Daily shape (60 min):**
- 0–5 min — Recall: 1 question from yesterday (spaced repetition, feeds the mistake log)
- 5–35 min — New concept (compact mode) + gotchas, one question at a time
- 35–55 min — Hands-on: Colab/Kaggle code or math sums
- 55–60 min — Samsung Notes entry + mistake-log entry

| Day | Topic | Hands-on deliverable | AUTOSAR / domain bridge |
|-----|-------|----------------------|--------------------------|
| 1 | Python libraries & notebook workflow | NumPy/Pandas warm-up notebook | Layered SW arch (MCAL→App) = the data-pipeline stack |
| 2 | Statistics & probability | Describe a real dataset (CAN-bus-style signals) | Sensor noise, signal variance, fault thresholds |
| 3 | Linear algebra | Vectors/matrices in NumPy; dot products | ECU signal vectors, transformation matrices |
| 4 | Calculus & gradient descent | Code gradient descent from scratch | Control-loop convergence / PID intuition |
| 5 | ML workflow, preprocessing, validation, metrics | Full clean→split→scale→CV pipeline | V-model parallels; test/validation discipline |
| 6 | Linear regression | Predict a continuous signal end-to-end | Predict sensor value from inputs |
| 7 | Logistic regression | Binary classifier (fault / no-fault) | DTC fault detection from signal features |

**Weekly mini-project (portfolio piece):** A small notebook — *"Predicting ECU fault from sensor signals"* — using a public dataset, applying preprocessing → regression → metrics. This is the thing you show in interviews. We build it incrementally across the week.

## Tweaks applied to the tutor prompt
1. **Tiered feedback** — full 10-point review only for gotchas/important questions; a tight 4-point review for routine checks (so we stay fast and don't overwhelm).
2. **Domain bridge** — every concept gets one AUTOSAR/embedded connection where it genuinely fits.
3. **Weekly mini-project** — turns learning into a portfolio artifact (job + hands-on).
4. **Spaced repetition** — each day opens with one recall question from the previous day.

## Mistake log (weak areas tracked here)
- *(none yet — populated as we go)*
